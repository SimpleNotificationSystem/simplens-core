meta {
  name: Reset Provider Rate Limit
  type: http
  seq: 9
}

post {
  url: {{process.env.BASE_URL}}/api/providers/{{process.env.PROVIDER_ID}}/rate-limit/reset
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
