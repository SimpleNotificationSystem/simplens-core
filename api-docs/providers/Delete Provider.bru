meta {
  name: Delete Provider
  type: http
  seq: 5
}

delete {
  url: {{process.env.BASE_URL}}/api/providers/{{process.env.PROVIDER_ID}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
