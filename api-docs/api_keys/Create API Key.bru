meta {
  name: Create API Key
  type: http
  seq: 2
}

post {
  url: {{process.env.BASE_URL}}/api/keys
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Integration Test Key",
    "type": "live",
    "scopes": ["*"]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
