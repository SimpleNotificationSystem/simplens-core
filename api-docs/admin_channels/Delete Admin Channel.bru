meta {
  name: Delete Admin Channel
  type: http
  seq: 8
}

delete {
  url: {{process.env.BASE_URL}}/api/admin-channels/{{process.env.ADMIN_CHANNEL_ID}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
