meta {
  name: Delete Channel Routing
  type: http
  seq: 4
}

delete {
  url: {{process.env.BASE_URL}}/api/channels/routing/{{process.env.ROUTING_CHANNEL}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
