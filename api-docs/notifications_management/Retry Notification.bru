meta {
  name: Retry Notification
  type: http
  seq: 4
}

post {
  url: {{process.env.BASE_URL}}/api/notifications/{{process.env.NOTIFICATION_ID}}/retry
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
