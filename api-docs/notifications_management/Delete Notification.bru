meta {
  name: Delete Notification
  type: http
  seq: 5
}

delete {
  url: {{process.env.BASE_URL}}/api/notifications/{{process.env.NOTIFICATION_ID}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
