# Server Configuration (HTTP Mode)
PORT=3001
ALLOWED_ORIGINS=*

# Local Usage (Stdio Mode)
# These are only required when running with --stdio flag locally
# In HTTP mode, these are passed per-request via headers
NS_API_KEY=your-api-key-here
SIMPLENS_CORE_URL=http://localhost:3000
